apiVersion: v1
kind: Service
metadata:
  name: edc-ui
  namespace: edc-ui
  labels:
    app: edc-ui
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: edc-ui
