version: '3'

services:
  edc-provider:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - edc-network

networks:
  edc-network:
    driver: bridge

